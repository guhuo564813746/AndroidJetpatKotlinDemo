mutation FeedbackAdd($title: String!,$version: String!,$model :String!,$content: String!,$pictureList: String!){
    feedbackAdd(title: $title,version: $version,model: $model,content: $content,pictureList: $pictureList){
        feedbackId
    }
}