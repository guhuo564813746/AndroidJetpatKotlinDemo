mutation FeedbackAddWithNoImg($title: String!,$version: String!,$model :String!,$content: String!){
    feedbackAdd(title: $title,version: $version,model: $model,content: $content){
        feedbackId
    }
}